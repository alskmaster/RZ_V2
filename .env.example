# ============================================================================
# Arquivo de exemplo de variáveis de ambiente (.env)
# Copie para `.env` e ajuste os valores conforme ambiente (DEV/TEST/PROD).
# ============================================================================

# --- Segurança / Sessão ---
# Chave secreta usada para assinar sessões e tokens CSRF
SECRET_KEY=troque-esta-chave-super-secreta

# --- Banco de Dados ---
# Exemplo local (SQLite)
DATABASE_URL=sqlite:///zabbix_reporter_v20.db
# Exemplo Postgres (ajuste host/porta/db/usuário/senha)
# DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/meubanco

# --- Zabbix ---
ZABBIX_URL=http://zabbix.example.com/api_jsonrpc.php
ZABBIX_USER=Admin
ZABBIX_PASSWORD=zabbix
# Opcional (apenas debug): ZABBIX_TOKEN=...

# --- Superadmin inicial ---
SUPERADMIN_PASSWORD=admin123

# --- Uploads / Relatórios ---
UPLOAD_FOLDER=uploads
GENERATED_REPORTS_FOLDER=relatorios_gerados
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif,html,pdf
# Tamanho máximo por request (bytes). 33554432 = 32 MB
MAX_CONTENT_LENGTH=33554432

# --- CSRF ---
# Normalmente manter TRUE
WTF_CSRF_ENABLED=true
# Tempo de expiração dos tokens (None = sem limite)
WTF_CSRF_TIME_LIMIT=None

# --- Sessão / Cookies ---
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax
# Em produção (HTTPS), ative:
SESSION_COOKIE_SECURE=true
# Tempo de vida da sessão (segundos). 28800 = 8h
PERMANENT_SESSION_LIFETIME=28800

# --- wkhtmltoimage ---
# Se instalado no PATH do SO, pode deixar vazio
WKHTMLTOIMAGE_PATH=/usr/bin/wkhtmltoimage

# --- Logging / Observabilidade ---
# Níveis: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
# Em produção, prefira JSON para coletores (true/false)
LOG_JSON=true

# --- Proxy / URL building ---
# Em ambiente com HTTPS por proxy (nginx/ingress), deixe "https"
PREFERRED_URL_SCHEME=https
# Defina o domínio se necessário (ex.: app.empresa.com)
# SERVER_NAME=app.empresa.com

# --- Flags de ambiente ---
# Ativar modo debug (apenas DEV)
DEBUG=false
# Ativar modo testing (para pytest/unittest)
TESTING=false
